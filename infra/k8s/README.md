# Kubernetes

`kind` cluster config and the Helm chart for the platform.

Grown by the `infra-scaffolder` agent at **P8**.

```
k8s/
├── kind-cluster.yaml
└── helm/
    └── loan-platform/
        ├── Chart.yaml
        ├── values.yaml
        ├── values-localstack.yaml
        ├── values-aws.yaml
        └── templates/
```
